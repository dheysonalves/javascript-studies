const ob_block = {}

ob_block.name = 'Dheyson Alves'
ob_block.salary = 9.000.toFixed(3)
ob_block.profession = 'Web Developer'

console.log(ob_block)

// Outra sintaxe

const ob_block_one = {
    name: "the cat",
    value: 9000
}